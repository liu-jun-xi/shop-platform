{
  "name": "shop-platform",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "npm install && npm install --prefix worker && npm install --prefix client",
    "build": "npm run build --prefix client",
    "dev:client": "npm run dev --prefix client",
    "dev:legacy": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "cf:dev": "npm run build && wrangler dev",
    "cf:deploy": "npm run build && wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply shop-db --local",
    "db:migrate": "wrangler d1 migrations apply shop-db --remote",
    "setup:cf": "npm run install:all && npm run build && npm run db:migrate:local"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wrangler": "^4.14.0"
  }
}
